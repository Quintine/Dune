# Homeworld charity and revival benefits

9 September 2026. This checkpoint extends the native-reserve runtime; it does not certify a complete Homeworld game or enable any unfinished start mode. The [source contract](HOMEWORLD_BENEFITS_RULES.md) distinguishes publisher rules, implementation inferences and unresolved combinations.

## Implemented behavior

- Primary native low population adds one to a qualifying charity claim and to the current free-revival rate. Salusa contributes neither addition. The shared quote supplies engine validation, player amounts and AI quantities.
- Charity keeps ordinary payment and the extra bank spice separate. CHOAM funds only the ordinary portion. Inflation doubles both portions or prevents collection. Canceling a Bene Gesserit claim pays neither portion. The production token triggers once on successful payment.
- A declared revival group uses the rate before that group returns. Later groups use the resulting population. The normal capacity is the greater of the ordinary limit and the current free rate, making the FAQ's four-free Fremen entitlement usable without granting a general fourth paid return. This formula and grouped timing are documented source compositions, not quoted errata.
- Fremen's alliance grant supplies three; the recipient's own low population adds its separate fourth. La La La suppresses the whole free rate. Existing paid discounts, typed allocation, special-counter limits and physical native return destinations remain separate.
- The actual entry to Revival records whether Tleilax starts low, before phase-opening controls. Later population changes preserve that turn-bound observation. The penalty suppresses other factions' ordinary free-revival bank income, while retaining paid receipts, its own free-revival income and separately named Ghola income.
- Table controls explain payment sources, current low benefits and the phase-start income condition. The reference contains the same timing guidance. All four AI policies use the shared current allowance; they withhold the special-Karama action that awaits the unresolved overlap ruling.

## Interrupted decisions and saved games

Charity response totals and bank portions are validated in live controls and explicit saved parents, including Nullentropy Box, Richese gift, exchange and discard continuations. Payment independently checks the eligible quote. This closes a reproduced corrupt-save path in which a Box-suspended claim could pay nine instead of three spice after automatic response allowance.

Normal Homeworld revival settlement validates its saved free allocation against the shared allowance before forces return. Current legal interruptions cannot change native population or ordinary usage while that request is pending; La La La success abandons the request. This rejects invented free counters even when a forged paid price is arithmetically consistent. A future force-changing interrupt will require an explicit timing/provenance extension, rather than blindly recomputing an earlier accepted quote.

Missing, stale or malformed Tleilax opening receipts reject; recovery never invents phase-start population from current reserves. The receipt is included in ordinary-card discard signatures. In-memory production D1 tests cover competing allowances/cancellations, restarts, private projections, rejected writes and preservation of another room.

## Evidence and limits

The complete offline check passed **3,190 tests**, including typecheck and lint. The final focused Homeworld/reference run passed **144 cases**. The production build and **40 live HTTP tests** passed. Seven new Homeworld benefit recovery cases cover production SQL and optimistic concurrency. The charity interruption regression uses genuine base setup with an explicitly labeled canonical Richese component seam; CHOAM payer cases similarly identify their unsupported-faction seam. They do not establish complete expansion setup.

Independent review found and integration fixed the AI/server mismatch, invented-free allocation and Box-suspended payout defects. A separate review fixed a shuffle-dependent Hajr fixture by locating genuine cards in either deck or dealt hands; a deterministic opponent-held-card regression preserves inventory.

An isolated Chrome QA room, `4K3HXJ6D`, passed desktop (1440×1000) and phone (390×844) inspection. A keyboard charity claim paid three spice, refresh retained it, an invalid four-force selection was disabled, and the accepted three-force return cost zero and raised Caladan from five to eight reserves. Refresh preserved the result and the opponent’s hand remained private. The inspected controls had no horizontal overflow or page errors. This is bounded feature acceptance, not complete module gameplay. Screenshots are `/tmp/dune-benefits-charity-desktop.png`, `/tmp/dune-benefits-charity-phone.png`, `/tmp/dune-benefits-revival-desktop.png` and `/tmp/dune-benefits-revival-phone.png`.

The prior development server was absent. A fresh read-only snapshot and SQLite backup preserved **2,648 existing rooms** before localhost startup; all versions and state hashes were unchanged afterward. The server returned HTTP 200. No database reset or recurring automation was used.

Pending material interpretations remain CHOAM's own low-population opening income, the Tleilaxu special-Karama/Homeworld overlap, and Ix high-population Suboid bonus quota/shortage behavior. Invasions, Homeworld battles, occupation, remaining card effects, full module combinations and complete games are unfinished. Keep public Homeworld/Advanced/unfinished expansion starts disabled.
